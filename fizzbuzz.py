# start = initial_starting_number
# end = final_number
# index = 0
# numbers_between = end - start

# check if numbers_between is positive or negative
# if number_between > 0:
#	if start + index is not divisible by 3
#		print start + index 
